
return {
    server_info = require('nats.protocol.server_info'),
    constants = require('nats.protocol.constants'),
    command = require('nats.protocol.command'),
    parser = require('nats.protocol.parser')
}
